#include<stdio.h>
#include<string.h>
int main()
{
	char str[100];
	int i,len;
	printf("enter the string:");
	fgets(str,sizeof(str),stdin);
	len=strlen(str);
	//reverse string
	printf("reverse string");
	for(i=len-1;i>=0;i--)
	{
		printf("%c",str[i]);
	}
	printf("\n");
	return 0;
}
